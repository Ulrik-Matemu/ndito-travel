export const RectangleCard = ( { children }: { children: React.ReactNode } ) => {
    return (
        <div  className="border border-[#231f20] my-10 p-6">
            {children}
        </div>
    )
}